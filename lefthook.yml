pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm format
      stage_fixed: true
    typecheck:
      glob: "*.{ts,tsx}"
      run: pnpm tsc --noEmit
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm lint
